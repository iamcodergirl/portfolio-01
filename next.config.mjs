/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        styledComponents: {
            fileName: false,
            ssr: false,
        },
        
    }
};

export default nextConfig;
